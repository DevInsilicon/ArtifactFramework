name: ArtifactFramework
version: '1.0'
main: dev.insilicon.artifactFramework.ArtifactFramework # You are required by the license to maintain the dev.insilicon.artifactFramework package name.
api-version: '1.20'
prefix: AFW # You are required by the license to retain the prefix "AFW" in the plugin name.
load: STARTUP
authors: [ ModdedKid ] # You are required by the license to retain the author name "ModdedKid" in the author list. You may add additional authors.



commands:
  artifact:
    description: Main command for the Artifact Framework
    usage: /artifact


permissions:
  artifact.admin:
    description: Permission for the admin command
    default: op